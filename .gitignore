# QA / ferramentas
.playwright-mcp/
qa-*.jpeg
qa-*.png

# Prints de avaliações — só na máquina local, não vão para git/hospedagem
images/avaliacoes/

# Deploy (helper local)
.ftphost

# Credenciais de FTP — LOCAL ONLY, nunca no git nem na hospedagem
.ftpauth

# Credenciais do Supabase — LOCAL ONLY
.supabaseauth

# Credenciais do Beds24 — LOCAL ONLY
.beds24auth

# JSON cru capturado da API Beds24 (dado de hóspede real) — LOCAL ONLY
tools/beds24/fixtures/

# Artefatos de deploy com hash de senha (basic auth) — LOCAL ONLY
.deploy/

# Fontes pesadas de imagem — masters locais, não vão para git nem hospedagem
images/quartos/*.png
*.psd

# SO
Thumbs.db
.DS_Store

# Python
__pycache__/
*.pyc
